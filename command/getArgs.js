"use strict";

/**
 * 获取各指令的参数
 */
class getArgs {


    /**
     * 获取Score指令的参数
     * @param {String} argString 
     * @returns {Array<String>} 参数集合
     */
    static getScoreArgs(argString) {
        let args = [];
        return args;
    }
}